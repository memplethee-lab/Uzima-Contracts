{
  "scripts": {
    "events:generate": "node scripts/events/generate-registry.mjs",
    "events:validate": "node scripts/events/validate.mjs",
    "test:pyramid:report": "node scripts/testing/pyramid-report.mjs",
    "test": "jest",
    "test:escrow": "jest tests/escrow.test.ts",
    "test:coverage": "jest --coverage"
  },
  "dependencies": {
    "@stellar/stellar-sdk": "^14.4.3",
    "ajv": "^8.12.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^20.0.0",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.0.0"
  }
}
